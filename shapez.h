#ifndef SHAPEZ_H
#define SHAPEZ_H
#include "encrypt.h"

int miner(int shapez);
int rotator(int shapez);
int cutter(int shapez);
int stacker(int shapezA, int shapezB);
int mixer(int colorA, int colorB);
int painter(int shapeA, int color);
int Target(int task);

#endif // SHAPEZ_H
